<Window x:Class="TelemetryViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TelemetryViewer"
        mc:Ignorable="d"
        Title="Telemetry Viewer" Height="450" Width="800" WindowState="Maximized">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="17*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar with Control Panels (15%) -->
        <Border Grid.Column="0" Background="#F5F5F5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="0">
                    <!-- Connection Panel -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="Connection" FontWeight="SemiBold" Background="#1F3B5C" Foreground="White" Padding="10,8" FontSize="13"/>
                        <StackPanel Margin="10">
                            <TextBlock Text="Port:" Margin="0,0,0,4"/>
                            <ComboBox x:Name="PortComboBox" Margin="0,0,0,10" MinWidth="120"/>
                            <TextBlock Text="Baud Rate:" Margin="0,0,0,4"/>
                            <ComboBox x:Name="BaudRateComboBox" Margin="0,0,0,10" MinWidth="120" IsEnabled="False"/>
                            <Button x:Name="ConnectButton" Content="Connect" Height="28" Click="ConnectButton_Click"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Recording Panel -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="Recording" FontWeight="SemiBold" Background="#1F3B5C" Foreground="White" Padding="10,8" FontSize="13"/>
                        <StackPanel Margin="10">
                            <Button Content="Start Recording" Margin="0,0,0,6" Height="28"/>
                            <Button Content="Stop Recording" Height="28"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Display Settings Panel -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="Display Settings" FontWeight="SemiBold" Background="#1F3B5C" Foreground="White" Padding="10,8" FontSize="13"/>
                        <StackPanel Margin="10">
                            <CheckBox Content="Auto-scroll" Margin="0,0,0,6"/>
                            <CheckBox Content="Show grid" Margin="0,0,0,6"/>
                            <TextBlock Text="Update Rate (Hz):" Margin="0,6,0,4"/>
                            <Slider Minimum="1" Maximum="60" Value="30"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- GridSplitter for resizing -->
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

        <!-- Worksheet Area (85%) -->
        <Border Grid.Column="2" Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="13*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#E9EEF5" Padding="10,6" Margin="0,0,0,10">
                </Border>

                <Border Grid.Row="1" Background="#FFFFFF">
                    <Grid>
                        <!-- Add your worksheet content here -->
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
