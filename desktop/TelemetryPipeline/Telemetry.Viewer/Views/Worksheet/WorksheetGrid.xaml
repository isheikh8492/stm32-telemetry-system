<UserControl
    x:Class="Telemetry.Viewer.Views.Worksheet.WorksheetGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:wks="clr-namespace:Telemetry.Viewer.Views.Worksheet">
    <UserControl.Resources>
        <!-- 40-DIP grid lines drawn over a WhiteSmoke base. Used by the
             Canvas Background when Worksheet.IsGridVisible is true. -->
        <DrawingBrush x:Key="GridBrush"
                      TileMode="Tile"
                      Viewport="0,0,40,40"
                      ViewportUnits="Absolute">
            <DrawingBrush.Drawing>
                <GeometryDrawing Brush="WhiteSmoke">
                    <GeometryDrawing.Geometry>
                        <GeometryGroup>
                            <RectangleGeometry Rect="0,0,40,40" />
                            <LineGeometry StartPoint="0,0" EndPoint="40,0" />
                            <LineGeometry StartPoint="0,0" EndPoint="0,40" />
                        </GeometryGroup>
                    </GeometryDrawing.Geometry>
                    <GeometryDrawing.Pen>
                        <Pen Brush="#E0E0E0" Thickness="0.5" />
                    </GeometryDrawing.Pen>
                </GeometryDrawing>
            </DrawingBrush.Drawing>
        </DrawingBrush>
    </UserControl.Resources>
    <ScrollViewer
        HorizontalScrollBarVisibility="Auto"
        VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding Plots}"
                      PreviewMouseLeftButtonDown="OnCanvasPreviewMouseLeftButtonDown">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas x:Name="WorksheetCanvas" Width="2000" Height="7000">
                        <Canvas.Style>
                            <Style TargetType="Canvas">
                                <!-- Solid background so clicks register on
                                     the empty canvas (Transparent works
                                     because mouse hit-testing only requires
                                     a brush, not visible color). -->
                                <Setter Property="Background" Value="WhiteSmoke" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DataContext.IsGridVisible, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Value="True">
                                        <Setter Property="Background" Value="{StaticResource GridBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Canvas.Style>
                    </Canvas>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left"   Value="{Binding X}" />
                    <Setter Property="Canvas.Top"    Value="{Binding Y}" />
                    <Setter Property="Panel.ZIndex"  Value="{Binding ZIndex}" />
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <wks:PlotItemHost />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
